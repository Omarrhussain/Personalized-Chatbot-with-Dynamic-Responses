class QueryRewriter:
    def rewrite(self, query: str) -> str:
        """
        Rewrites a query to improve retrieval performance.
        """
        # Simplistic rewrite: just return the query for now
        return query.strip()
